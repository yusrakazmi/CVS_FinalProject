
// We are assuming that the computing will be done at server side.
export interface Account {
    accountNumber: string
    availableCash: number
    todaysChange: number
    percentageChange: number
}

